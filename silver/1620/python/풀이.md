우리 포켓몬 마스터 이다솜 씨를 위해 도감을 출력하는 프로그램을 작성한다.
n개의 포켓몬 이름과 m개의 도감 번호 OR 포켓몬 이름 이 입력되는데
위의 n개의 포켓몬 들의 이름 OR 도감 번호를 출력한다.
name -> number
number -> name 으로 출력한다.

입력을 최적화 하기 위해
input = sys.stdin.readline 를 실행한다.

또한 딕셔너리를 통해 key : value 를 기준으로 사용한다.
매커니즘은 c++ 과 동일하게 진행한다.