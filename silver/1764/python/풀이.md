듣지 못한 사람 n과 보지 못한 사람 m 이 주어진다.
즉 N의 input과 M의 input 중 중복될 경우 사전 순으로 출력한다.
이건 그냥 set 으로 진행해 자동 정렬까지 추가한다. X
c++의 set 은 Red-Black-Tree 기반이여서 자동 정렬이 되지만
python의 set 은 Hash-Table 기반이여서 자동 정렬이 되지 않는다.

메인 로직은 c++ 과 동일하다.
