첫 줄에 n, m이 입력된다.
n개의 문자열을 입력된다.
m개의 문자열을 입력된다.


n개의 문자열들의 집합을 S로 칭하며
m개의 문자열이 집합 S에 있을 경우 count +1 을 한다.



c++ 의 메인 알고리즘으로 unordered_set을 사용한다.
unordered_set는 해시 테이블 기반 집합이다.
insert로 데이터를 추가하고
erase 로 삭제한다.
count 로 존재 여부를 확인 후 (1, 0 으로 반환.)
find != end로 존재 여부를 확인한다. (iterator 로 주소값 반환)




