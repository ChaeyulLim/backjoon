n개의 X좌표가 입력된다.
각 인덱츠대로 자신보다 작은 값들을 출력한다.
그냥 뺑뺑이 돌리면 되지 않을까 싶은데 문제는 시간과 메모리이다.
한번 해보자. 안된다.
정렬하는데, 값은 index, value 로 주는 접근을 하는건 어때?
그렇게 하고 정렬하면 valude 값대로 그 전의 값들의 count 를 할 수 있잖아.


핵심 로직은 다음과 같다.

int, int 형태로 pair 를 통해 묶는다.
각각 index와 value의 값을 가진다.

정렬은 value를 기준으로 sort 알고리즘을 활용한다.
반복문을 돌려 i를 기준으로 정렬된 value의 값이 들어간 index에 counting 된 값을 적재한다. 적재 후 count 를 +1 한다.

만약 이전과 같은 값을 가지면 count는 증가시키지 않는다.

count는 이전의 값들이 몇개가 있는지 체크하는 용도이다.
