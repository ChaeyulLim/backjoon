2개의 입력을 계속해서 더해주는 것 같다.
다만 10952처럼 종료 조건 없이 그냥 전부 출력하는 듯 하다.
입력 부분의 std::cin 부분의 반환은 출력이 있는가 없는가를 확인하는 듯 하다.
입력이 더 이상 없으면 EOF 상태를 출력해 false 형태가 되는 듯 하다.


문제의 핵심은 적혀있지 않지만
EOF 즉 입력이 끝날때 를 캐치해야되는 것 같다.
c++에선 EOF를 잡기 위해선 std::cin 의 반환값이 필요하고
python 에선 try and except 를 사용해서 Error 를 캐치하는게 핵심인 듯 하다.