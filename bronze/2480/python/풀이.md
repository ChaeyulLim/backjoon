같은 눈 3 : 10000 + 눈 * 1000
같은 눈 2 : 1000 + 같은 눈 * 100
전부 다른 눈 : 가장 큰 눈 * 100

로직은 간단한데 같은 눈을 판단하고 가장 높은, 혹은 몇개가 있는지를 확인하는 로직이 고민이 된다.
그냥 단순히 전부 할 때 마다 비교를 하게 되면 메모리와 코드 속도는 상승하겠지, 하지만 주사위의 개수가 커질 경우엔 정렬 후 set 으로 보는게 맞지 않을까 싶다.
아니면 이렇게 하는건? 찾으면서 같은 눈, 가장 큰 눈, 같은 개수 이렇게 3개의 값을 가져올 수 있으면?
자자 단순히 and 연산자와 or 연산자로 동시에 확인하면서 들어간다
a, b, c
a == b && a == c && b == c
a == b || a == c || b == c
else 
로 진행한다.