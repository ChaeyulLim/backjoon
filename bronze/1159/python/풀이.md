농구 경기 명단을 출력하는 프로그램을 작성하라인데
입력 받은 데이터를 관리 할 수 있도록 dic 을 사용하고 그를 정렬할 때 sort 를 사용한다.
key: 이름의 첫 글자, value: 중복 counting

선수는 150명 이하이므로 순회를 하면 충분할 것이다.


dict 용 정렬이 있는줄 처음 알았네
`dict(sorted(result.items(), key=lambda x: x[0]))`
