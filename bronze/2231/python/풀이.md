분해 합이란
N의 값이 주어지면 M의 값을 찾으라는 소리가 된다.
M = M + (M[0] + M[1] + m[2]...) = N 이 되는 수식을 말한다.
즉 노가다 해서 원하는 값을 찾으란 소리인 것 같다;
기준치는 1 ~ 1_000_000까지 찾으라는 것 같다.
아니지 입력값보다 M이 클 수 없으니 N만큼만 더하면 될 것 같다.

음.. 반복문을 최소한으로 하려면 n / 2 ~ n까지 수행하면 될 것 같은데?
아무리 작은 수를 더한다고 하더라도 /2 이하는 절대로 안나올 것 같다.