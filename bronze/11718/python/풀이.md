c++ 에선 입력이 getline 으로 진행해야될 것 같다.
cin 으로 입력을 받게 될 경우 띄어쓰기를 할 때 뒷부분은 잘리므로
getline 을 통해 입력받고 출력한다.

이전에 비슷한 문제로 입력의 끝이 정해지지 않은 경우에
while 로 true를 넣어 무한 반복하고 try and except 를 사용하여 에러 구문을 해결하는 방향으로 진행하자.
exit(0)은 정상 종료이고 exit(1) 은 에러 출력 후 종료인 듯 하다.
exit(1)을 하니까 백준에선 컴파일 에러를 출력하네.