숫자를 전부 입력 받고
문자열로 입력 받고 전부 string 으로 변환한다.
한글자씩 때서 진행하고 전부 더한다.

static_cast<int>를 하게 되면 char -> int 이기에 아스키 코드로 변경된다.
하지만 숫자는 0 : 48, 1 : 49 ... 로 바뀌기 때문에 -48을 연상해주었다.
물론 이를 안정적으로 바꿔주는 string 메소드인 std::stoi() 라는 메소드가 존재하지만
해당 문제에선 이걸로도 충분 할 것 같아 사용하지 않았다.